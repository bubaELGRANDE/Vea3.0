export interface IOrderDetailCustomer {
  id: number;
  name: string;       // ej. "Fernando Ruiz"
  avatarUrl?: string;  // URL de la imagen de perfil
  email: string;      // ej. "fern_ruiz2gmail.com"
  phone: string;      // ej. "+503 75033693"
}