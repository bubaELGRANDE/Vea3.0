export interface IMetricsSummary {
  monthlySales: number;       // Corresponde a la tarjeta "Ventas del mes"
  pendingOrders: number;      // Corresponde a "Pedidos Pendientes"
  activePublications: number; // Corresponde a "Publicaciones activas"
}

